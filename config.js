/* ==========================================================================
   UEnvision — database configuration
   --------------------------------------------------------------------------
   Fill these in once you've created your free Supabase project and run the
   table setup in schema.sql (see the SQL editor in your Supabase dashboard):

     1. Go to supabase.com -> your project -> Project Settings -> API.
     2. Copy the "Project URL" into UE_SUPABASE_URL below.
     3. Copy the "anon public" key into UE_SUPABASE_ANON_KEY below.

   Until these are filled in, the app still works — survey submissions are
   just saved to the browser's local storage instead of your database, so
   you can keep building and testing before your Supabase project exists.
   ========================================================================== */

window.UE_SUPABASE_URL = '';
window.UE_SUPABASE_ANON_KEY = '';
